[package]
name = "kivo"
version = "0.8.1"
description = "Desktop helper for the Kivo GPIO keyboard"
edition = "2024"
default-run = "kivo"

[lib]
name = "kivo_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.5.6", features = [] }

[dependencies]
base64 = "0.22"
notify = "8.2"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml_ng = "0.10"
sha2 = "0.10"
rusqlite = { version = "0.37", features = ["bundled"] }
nusb = "0.2.5"
serialport = { version = "4.9", default-features = false }
tauri = { version = "2.11.5", features = ["image-png", "tray-icon"] }
tauri-plugin-dialog = "2.7.2"
tauri-plugin-log = "2.9.0"

[features]
default = []
product-studio = []
product-cli = ["product-studio"]

[[bin]]
name = "kivo-product"
path = "src/bin/kivo-product.rs"
# Keep the standalone product builder out of the Studio app build. It is only
# compiled when the explicit product-cli feature is requested.
required-features = ["product-cli"]

[target.'cfg(target_os = "windows")'.dependencies]
windows-sys = { version = "0.59", features = [
    "Win32_Foundation",
    "Win32_Storage_FileSystem",
    "Win32_System_DataExchange",
    "Win32_System_Memory",
    "Win32_System_Ole",
    "Win32_UI_Shell",
    "Win32_UI_WindowsAndMessaging",
] }

[dev-dependencies]
tempfile = "3"
tauri = { version = "2.11.5", features = ["test"] }
