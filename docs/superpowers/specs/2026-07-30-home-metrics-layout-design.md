# 首页指标布局

## 范围

- 最近 7 天按当前型号的真实 `groups`、`columns` 与按钮顺序渲染；没有统计的按键保留为空白。
- 按键统计使用浅绿色阶，仍按当前最大次数归一化。
- 将日志 `DIGIT_5 pressed` 显示为 `按下 DIGIT_5`。
- 将设备型号选择移至顶栏最右侧，全部页面共用；移除侧栏语言选择，界面固定使用简体中文。

## 非范围

- 不修改型号配置、已保存语言设置、设备协议或运行时事件格式。
- 不增加语言选择的替代控件。

## 验证

- 前端测试覆盖按真实布局渲染、中文日志与不再出现两个原生选择框。
