watch_file .nvmrc

if [[ -z ${NVM_DIR:-} || ! -s "$NVM_DIR/nvm.sh" ]]; then
  echo "Kivo requires nvm; set NVM_DIR before loading direnv." >&2
  return 1
fi

source "$NVM_DIR/nvm.sh"
nvm use --silent
PATH_add "$NVM_BIN"
