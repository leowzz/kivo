[project]
name = "kivo"
version = "0.5.9"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pip>=25,<26",
    "platformio>=6.1,<7",
    "prompt-toolkit>=3.0,<4",
    "pyserial>=3.5,<4",
    "pytest>=8,<9",
]

[tool.pytest.ini_options]
pythonpath = ["."]
